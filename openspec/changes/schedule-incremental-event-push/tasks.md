## 1. 配置与数据迁移

- [x] 1.1 在代码默认配置和 `src/config/app.json` 示例中增加定时增量时区、默认发送时刻列表与调度轮询周期，并接入既有环境变量覆盖和配置来源展示
- [x] 1.2 为发送时刻列表、时区和轮询周期实现规范化与边界校验，保证旧配置缺少字段时可使用默认值启动
- [x] 1.3 为 `notification_rules` 编写可重复迁移，增加投递模式、发送时刻、启用/下次运行时间和三元组游标字段，并将所有旧规则迁移为 `immediate`
- [x] 1.4 创建定时增量运行、收件人批次和事件明细表，补齐状态约束、外键、计划槽/事件幂等唯一约束与到期查询索引
- [x] 1.5 增加迁移测试，验证空库、已有即时规则、重复启动、历史投递保留和新增表索引均正确

## 2. 规则模型与高水位服务

- [x] 2.1 扩展规则读取、创建和更新逻辑，规范化 `delivery_mode` 与 `send_times`，并让即时入队路径只匹配 `immediate` 规则
- [x] 2.2 实现 `task_run_events` 三元组高水位读取、词典序上下界谓词和按事件 ID 去重的候选查询
- [x] 2.3 在候选查询中落实任务、人物、事件类型、人物可用状态与审核未驳回条件，并为查询增加必要索引或执行计划检查
- [x] 2.4 实现创建、启用、模式切换和范围变更时的游标重置，名称/发送时刻变更时的游标保留及 `cursor_reset` 结果
- [x] 2.5 实现规则停用、重新启用和删除语义，确保停用期间不积累待补历史且既有运行记录保留
- [x] 2.6 增加规则兼容与生命周期单元测试，覆盖旧即时规则、无历史回溯、范围重置、只改时刻和同时间戳并列事件

## 3. 定时增量运行与调度器

- [x] 3.1 新增定时增量服务模块，实现北京时间每日多时刻的规范化、下一槽位计算及 UTC 持久化
- [x] 3.2 在短 `BEGIN IMMEDIATE` 事务中实现运行快照：固定上下界、插入去重候选、快照收件人、按上限分片、推进游标并更新下一时刻
- [x] 3.3 实现空窗口运行，使系统在不创建 SMTP 批次时仍原子推进游标并记录 `empty` 状态
- [x] 3.4 实现计划运行 `UNIQUE(rule_id, scheduled_at)` 冲突处理，保证重复轮询和并发触发返回同一结果且不重复推进游标
- [x] 3.5 实现“立即汇总”手工运行，使用当前游标到当前高水位并记录触发用户，且与计划运行并发时保持事务一致
- [x] 3.6 实现窗口预览，只返回游标、预期上界、总数与有限样例，且不创建运行或推进游标
- [x] 3.7 实现后台调度线程的启动、停止、异常隔离和重启恢复，错过多个槽位时只处理最近一期并从当前时间计算下一槽位
- [x] 3.8 增加调度与事务测试，覆盖每日多时刻、未到期、重启补跑、重复轮询、事务回滚、并发截止后事件和空窗口

## 4. 邮件构建、Worker 与运行状态

- [x] 4.1 实现定时增量邮件文本/HTML 模板，显示入库增量窗口、事件数量、北京时间和证据链接，并按发生时间/人物/类型/ID 稳定排序
- [x] 4.2 复用 `max_events_per_message` 为每个收件人确定性分片，并为每个批次生成稳定 `Message-ID`
- [x] 4.3 扩展 `NotificationWorker`，在即时、定时增量和日报批次间公平选取到期任务，并复用 SMTP、凭证解密、退避与错误清洗
- [x] 4.4 实现定时运行状态聚合及事件删除/不可见时的 `skipped` 明细处理，不在发送时动态补入其他事件
- [x] 4.5 实现终止失败批次的管理员重试，恢复原批次和原明细而不改变运行窗口或创建重复项
- [x] 4.6 增加邮件与 Worker 单元测试，覆盖内容排序、未知发生时间、分片、收件人快照、成功、暂时失败、终止失败、手工重试和稳定消息标识
- [x] 4.7 增加回归测试，确认即时推送和每日时间线邮件在 Worker 扩展后仍能独立入队、投递和恢复

## 5. FastAPI、权限与审计

- [x] 5.1 扩展推送规则请求/响应模型和 CRUD API，返回模式、发送时刻、下一运行时间、游标摘要及是否发生游标重置
- [x] 5.2 新增定时增量预览、立即汇总、运行分页列表、运行详情和失败批次重试 API
- [x] 5.3 为运行列表/详情实现规则、状态和时间筛选，返回候选/发送/失败/跳过计数与清洗后的错误摘要
- [x] 5.4 对查看接口应用 `notifications` 页面权限，对所有写接口应用管理员权限，并保持 SMTP 密码和凭证主密钥不出现在响应或日志
- [x] 5.5 为规则变更、游标重置、立即汇总和批次重试写入不含邮件正文/凭证的审计记录，并在任务中心/应用日志记录运行计数
- [x] 5.6 增加 API 集成测试，覆盖管理员完整流程、普通授权用户只读、无权限拒绝、输入校验、审计脱敏、分页筛选和幂等触发

## 6. Vue 推送管理界面

- [x] 6.1 在规则表单增加即时/定时增量模式和可增删的 `HH:mm` 发送时刻控件，仅在定时模式显示相关字段和北京时间说明
- [x] 6.2 在规则列表展示模式、发送时刻、下一次运行、上次增量窗口与启停状态，并在范围变更或重新启用将重置游标时要求确认
- [x] 6.3 增加待推窗口预览和管理员“立即汇总”操作，显示当前上下界、匹配总数和有限事件样例
- [x] 6.4 增加定时增量运行列表和详情视图，展示北京时间窗口、触发方式、各类计数、收件人批次、事件明细和清洗错误
- [x] 6.5 增加失败批次重试和成功/失败反馈，并保证普通授权用户看不到修改、立即汇总或重试按钮
- [x] 6.6 完成桌面与移动布局、键盘可用性、加载/空/错误状态和现有推送页面视觉一致性调整
- [x] 6.7 增加前端测试，覆盖模式条件表单、时刻编辑、确认提示、预览、立即汇总、运行详情、权限只读和响应式标记

## 7. 文档、运维与 Jenkins

- [x] 7.1 更新 `软件需求规格说明书.md`，补充即时/定时增量模式、入库高水位、恢复、权限和验收场景
- [x] 7.2 更新 `系统设计说明书.md`，记录数据模型、事务边界、调度生命周期、至少一次 SMTP 语义、迁移和回滚方案
- [x] 7.3 更新根 `README.md` 的系统介绍、推送管理页面、配置说明、部署、运维、访问和定时增量使用说明
- [x] 7.4 检查并更新 `.gitignore`、Linux/Windows 启动/停止/状态脚本及 systemd 服务说明，确认调度线程随应用正确启停且 `data` 未被忽略
- [x] 7.5 检查并更新 `src/JenkinsConfig/Jenkinsfile`，保留 SSH SCM 与定期检查要求，确保增量部署不覆盖现有 `src/config/app.json` 并纳入新增测试

## 8. 测试、部署与验收

- [x] 8.1 运行后端完整单元/API 集成测试并修复回归，记录通过结果
- [x] 8.2 运行前端完整测试和生产构建并修复回归，记录通过结果
- [x] 8.3 执行本地烟雾测试：创建定时规则、跨两个高水位入库事件、到期汇总、空窗口、SMTP 失败重试和重启恢复
- [x] 8.4 使用真实服务烟雾测试验证登录、权限、系统配置、任务中心、即时推送、定时增量推送和日报互不干扰
- [x] 8.5 验证首次部署创建 `data/password.txt`，增量部署保留数据库和 `app.json`，并验证 Linux systemd 与 Windows/Linux 运维脚本
- [ ] 8.6 自测通过后提交并推送 GitHub，手工触发 Jenkins 构建，确认流水线测试/部署成功并请用户访问部署服务验收定时增量窗口
