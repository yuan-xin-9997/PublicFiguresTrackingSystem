## 1. 后端：事件主体归属

- [x] 1.1 在 `extractor.py` 中实现保留段落/列表边界的子句切分、谓词定位和姓名/别名主体窗口工具，并为每次拒绝返回可记录的原因
- [x] 1.2 用逐谓词主体绑定替换现有文档级 `_primary_person_id` 归属逻辑，移除“仅标题命中或仅姓名命中即生成 other”的兜底
- [x] 1.3 实现受限的“他/她”相邻句承接规则，遇到多人物、跨段落、标题或日期列表边界时拒绝自动归属
- [x] 1.4 把同一主体证据后置校验接入本地抽取、外部模型响应和外部模型失败回退路径，并升级提示词/Schema 版本标识
- [x] 1.5 将归属候选、接受、拒绝及原因计数从抽取结果传到模型运行记录和采集任务日志

## 2. 后端：文章正文质量

- [x] 2.1 在 `collectors.py` 中增加仅匹配 `chinadaily.com.cn` 域族的正文开始、重复标题、页头导航、推荐阅读和页尾清洗规则
- [x] 2.2 扩展文章有效性检查，覆盖中国日报首页/频道/专题聚合页、异常标题、导航密集、重复污染和清洗后正文过短，并返回稳定拒绝原因
- [x] 2.3 确保 WebFetch `generic.article` 成功结果与 artifact/body 本地清洗降级结果都经过同一清洗和拒绝管线，且不新增目标站点直连
- [x] 2.4 在网站发现和采集运行日志中增加清洗后接受、聚合页拒绝、正文不足拒绝和失败计数，不记录 API Key 或敏感配置

## 3. 后端：维护服务与 API

- [x] 3.1 实现可按人物、来源或全量分页扫描的存量主体证据重验服务，输出材料、无效证据、保留事件、孤立事件、锁定跳过项和有限样例
- [x] 3.2 实现存量中国日报正文质量扫描，复用新版清洗/拒绝规则并计算证据和事件影响
- [x] 3.3 增加管理员专用 dry-run/执行 API；执行时在 SQLite 事务内先移除无效证据，只删除失去全部有效证据且未人工锁定的事件
- [x] 3.4 为维护执行写入审计日志和结构化结果，验证失败回滚、重复执行幂等，并拒绝普通用户访问

## 4. 前端

- [x] 4.1 在任务中心增加“事件归属重验”和“中国日报污染清理”的管理员入口、人物/来源可选范围、dry-run 预览计数及有限样例
- [x] 4.2 为真实执行增加明确二次确认、加载/失败/成功反馈，并隐藏普通用户的维护操作
- [x] 4.3 保持现有时间线筛选、来源显示、权限导航和北京时间格式不变，完成受影响页面回归检查

## 5. 配置

- [x] 5.1 复核 `src/config/app.json` 与配置加载/脱敏逻辑，继续从现有 collector 配置读取 WebFetch 地址、API Key 环境变量、超时、缓存和直连降级策略
- [x] 5.2 确认新增站点适配不包含环境 IP、端口、用户名、密码或绝对路径；若增加批次大小/样例上限配置，同步默认值、范围校验和系统配置展示
- [x] 5.3 验证登录、password.txt 用户同步、admin/user 页面权限及系统配置敏感字段脱敏没有回归

## 6. 数据迁移与存量处理

- [x] 6.1 优先复用现有 `raw_documents`、`timeline_events`、`event_evidence`、`model_runs`、`task_logs`、`event_history` 和 `audit_logs`；若确需字段变更，实现向后兼容 SQLite 迁移
- [x] 6.2 为任何新增迁移补充旧库升级、重复启动和数据保留测试，确保 `human_locked` 与历史证据不丢失
- [x] 6.3 制定生产执行清单：备份 `src/data/app.sqlite3`、先 dry-run 保存样例、经确认后执行、重跑受影响来源并抽样检查习近平时间线

## 7. 测试

- [x] 7.1 增加本地抽取单元测试：正确的习近平主体、扎帕罗夫会见王毅、王毅会见伊朗外长、背景引用、仅姓名命中、明确/不明确代词和真实 other 事实
- [x] 7.2 增加外部模型及回退测试：错误 `person_id` 被后置校验拒绝、证据不在原文被拒绝、外部失败回退与本地规则一致
- [x] 7.3 增加中国日报采集测试：用户给出的“万山磅礴看主峰｜习近平谈亚太合作”页面框架、页尾推荐、合法正文、首页/频道拒绝、子域匹配和非中国日报转载隔离
- [x] 7.4 增加维护 API 测试：管理员 dry-run 无写入、普通用户 403、共享有效证据保留、孤立事件删除、人工锁定保护、事务回滚、审计日志和重复执行幂等
- [x] 7.5 增加前端任务中心测试，覆盖管理员预览/确认执行、错误反馈、普通用户不可见及北京时间/时间线回归
- [x] 7.6 运行全部后端单元/API 测试、前端单元与构建测试及 `src/tests/smoke_live.py`，修复所有回归并记录通过结果

## 8. 文档

- [x] 8.1 更新 `README.md` 的采集、人物主体归属、任务中心维护和运维章节，并检查系统介绍、页面、配置、部署、运维和访问方式仍完整
- [x] 8.2 更新 `软件需求规格说明书.md`，加入主体证据、仅提及拒绝、中国日报正文质量、存量修复权限与验收案例
- [x] 8.3 更新 `系统设计说明书.md`，记录统一后置校验、中国日报适配位置、dry-run/事务/人工锁定保护及回滚策略
- [x] 8.4 检查 `.gitignore` 继续忽略 logs 但不忽略 data，并确认没有把真实凭证、数据库备份或运行日志提交到仓库

## 9. 部署与 Jenkins

- [x] 9.1 检查并按需更新 `src/JenkinsConfig/Jenkinsfile`，确保流水线运行新增后端、前端、构建、迁移和烟雾测试
- [x] 9.2 在 Linux 验证 `start.sh`、`status.sh`、`stop.sh`，并在可用的 Windows/Jenkins 环境验证对应 PowerShell 脚本或流水线步骤
- [x] 9.3 部署前备份生产 SQLite，部署后先运行 dry-run 并人工核对反例，再执行存量修复和来源重分析
- [x] 9.4 提交并推送 GitHub 后手动触发 Jenkins 构建，确认成功，再请用户访问 `persontracking.yuan-xin.top` 验证习近平时间线与中国日报文章结果
