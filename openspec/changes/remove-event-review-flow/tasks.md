## 1. 数据迁移与默认状态

- [x] 1.1 梳理 `timeline_events.review_status` 当前写入、查询、统计、通知和测试引用点，确认需要移除或兼容的路径
- [x] 1.2 增加兼容迁移或启动修复，将 `pending`、`needs_review` 等待审核历史事件规范化为 `approved`，并保留 `rejected` 不重新进入默认可见集合
- [x] 1.3 调整事件创建、人工录入分析、重分析、合并或导入逻辑，使可保存的新事件默认写入 `approved`
- [x] 1.4 确认删除事件采用现有硬删除或软删除语义，并统一时间线、地图、搜索、人物详情、通知和日报的排除条件

## 2. 后端 API、权限与审计

- [x] 2.1 移除或废弃 `/events/{id}/review` 审核操作路径，必要时返回明确的废弃/不可用响应
- [x] 2.2 实现或完善事件删除 API，要求管理员权限、后端存在性校验和安全错误响应
- [x] 2.3 为事件删除写入审计日志，记录操作者、事件 ID、人物、标题摘要和删除时间，避免记录敏感凭证或无关环境信息
- [x] 2.4 调整仪表盘、任务中心和系统配置 API，移除待审核队列统计、跳转和审核中心页面依赖
- [x] 2.5 调整事件列表、事件详情、人物详情、地图和搜索 API，移除审核状态筛选或将其兼容为无效参数
- [x] 2.6 调整即时推送、定时增量推送和每日时间线邮件候选筛选，使默认通过事件可发送且发送前跳过已删除事件
- [x] 2.7 更新权限管理默认页面集合和用户页面同步逻辑，清理或忽略历史 `review`/审核中心页面项

## 3. Vue 前端

- [x] 3.1 删除审核中心页面、导航入口、页面切换分支、空态、表格和相关状态变量
- [x] 3.2 更新权限管理页面，移除审核中心权限选项，并保证历史用户配置中残留页面不会导致渲染异常
- [x] 3.3 更新时间线筛选工具栏和事件卡片，移除审核状态筛选与待审核视图，保留确认状态、置信度和来源证据展示
- [x] 3.4 在时间线卡片和事件详情中为管理员增加删除事件按钮、确认提示、加载/错误状态和删除后列表刷新
- [x] 3.5 移除事件详情中的通过审核和驳回按钮，并调整普通用户只读视图
- [x] 3.6 更新仪表盘待审核统计、最近事件、任务中心和审计日志相关文案，使页面不再指向审核中心
- [x] 3.7 检查桌面和移动端布局，确保删除按钮、筛选控件和事件详情内容不重叠、不撑破容器

## 4. 后端与前端测试

- [x] 4.1 增加数据库迁移测试，覆盖待审核历史转通过、历史驳回不重新发布和重复迁移幂等
- [x] 4.2 增加事件创建/抽取单元测试，验证低置信、冲突和可保存事件默认通过并进入时间线
- [x] 4.3 增加事件删除 API 测试，覆盖管理员成功删除、普通用户 403、事件不存在、审计日志和列表排除
- [x] 4.4 更新通知与日报测试，验证新事件不等待审核即可入选，发送前已删除事件被跳过
- [x] 4.5 更新仪表盘、任务中心、权限和事件查询 API 测试，移除待审核队列和审核状态筛选断言
- [x] 4.6 增加 Vue 测试，覆盖无审核中心导航、无审核权限选项、无审核详情按钮、管理员删除事件和普通用户不可删除
- [x] 4.7 更新前端布局测试或快照，覆盖时间线删除交互在桌面与移动端的显示

## 5. 冒烟、文档与 Jenkins

- [x] 5.1 更新 `README.md` 的系统介绍、页面介绍、事件流程、权限、通知/日报和运维说明，移除审核中心描述
- [x] 5.2 更新 `软件需求规格说明书.md`，改写功能列表、流程图、权限表、时间线、事件详情、审计、接口、验收标准和冒烟测试
- [x] 5.3 更新 `系统设计说明书.md`，改写架构图、事件分析与质量控制、数据模型、API、前端页面、审计和测试策略
- [x] 5.4 更新真实服务冒烟脚本，验证启动、登录、采集/录入、时间线默认可见、管理员删除事件、普通用户拒绝删除和停止服务
- [x] 5.5 复核 `src/JenkinsConfig/Jenkinsfile`，确保执行新增后端测试、前端测试、生产构建、冒烟脚本和 OpenSpec 校验
- [ ] 5.6 验证 Linux/Windows `start`、`status`、`stop` 脚本不依赖审核中心流程，且日志中能看到删除事件审计

## 6. 完整验证与交付

- [ ] 6.1 运行完整后端 pytest、前端测试、前端生产构建、冒烟测试和 OpenSpec 严格校验并修复全部失败
- [x] 6.2 复核登录、权限管理、系统配置、任务中心、时间线、地图、搜索、即时推送、定时增量推送和每日时间线邮件无回归
- [ ] 6.3 提交并推送通过质量门禁的实现到 GitHub
- [ ] 6.4 手工触发 Jenkins 构建，确认部署和就绪检查成功
- [ ] 6.5 提供构建后的服务地址和验证步骤，请用户确认无审核中心、事件默认展示和时间线删除符合预期
