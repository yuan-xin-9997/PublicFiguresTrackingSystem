## 1. 配置与时间模型

- [x] 1.1 在后端默认配置和 `src/config/app.json` 增加 `notifications.daily_digest` 的时区、默认发送时间、默认窗口模式、默认最近小时数、最大小时数和轮询间隔
- [x] 1.2 实现日报配置规范化与校验，覆盖 `HH:mm`、IANA 时区、窗口模式、最近小时上下限及环境变量覆盖来源
- [x] 1.3 实现基于业务日期和配置时区的计划时刻、昨天自然日半开窗口、最近 N 小时窗口及 UTC 转换工具
- [x] 1.4 实现规则创建、启用和发送时间修改后的下一未来日历时刻计算，禁止使用完成时间加固定秒数

## 2. SQLite 数据迁移

- [x] 2.1 增加向后兼容迁移，创建 `daily_digest_rules`、`daily_digest_rule_persons` 和 `daily_digest_rule_recipients` 及必要索引
- [x] 2.2 创建 `daily_digest_runs`，保存计划业务日期、计划时刻、固化窗口、触发方式、状态和计数，并添加 `(rule_id, scheduled_date)` 唯一约束
- [x] 2.3 创建 `daily_digest_batches` 和 `daily_digest_items`，添加收件人分片、事件明细、稳定消息 ID、状态索引及幂等唯一约束
- [x] 2.4 验证全新数据库、现有邮件推送数据库升级和重复迁移均成功，且不改写现有即时推送与时间线数据

## 3. 后端规则与窗口预览

- [x] 3.1 实现日报规则列表、读取、创建、更新、启停和删除服务，校验至少一个可用人物、事件类型和有效收件人
- [x] 3.2 实现人物停用或软删除后的新日报排除语义，同时保留规则关联及历史运行展示
- [x] 3.3 实现下一计划窗口和指定业务日期预览服务，返回北京时间边界、匹配数量和有限样例且不产生投递副作用
- [x] 3.4 实现规则级空日报策略和默认昨日自然日、默认 `08:30` 的配置继承

## 4. 事件选择与时间线生成

- [x] 4.1 实现跨采集任务的日报候选查询，按规则人物、事件类型、非 rejected 状态和固化半开窗口筛选
- [x] 4.2 实现选择时间优先 `start_at`、缺失时回退 `created_at` 的窗口归属，并确保渲染仍标记事件时间未知
- [x] 4.3 实现 `start_at` 升序、未知时间置后、人物/类型/事件 ID 次级键的稳定排序
- [x] 4.4 在短事务中固化日报运行、按邮件上限稳定分片并写入收件人批次和事件明细
- [x] 4.5 实现发送前事件存在性与审核状态复检，跳过已删除或已驳回事件并安全记录原因
- [x] 4.6 生成 UTF-8 纯文本与 HTML 日报，包含规则、窗口、事件数、人物、类型、标题、摘要、北京时间或时间未知、地点、状态、具体来源和可选详情链接
- [x] 4.7 实现无匹配事件默认记录 `empty` 且不发信，并在 `send_when_empty` 启用时生成明确的“无动态”邮件

## 5. 日历调度、Outbox 与重试

- [x] 5.1 新增日报日历调度器并接入 FastAPI lifespan，按配置轮询到期规则并使用唯一约束领取计划运行
- [x] 5.2 实现应用重启后的最近到期运行恢复、跨多日停机仅补最近一期及更早日期错过记录
- [x] 5.3 实现指定业务日期的管理员补跑，限制在规则启用后日期并复用自动运行的窗口、选择和幂等逻辑
- [x] 5.4 抽取并复用现有 SMTP 传输、凭证解析、错误清洗、退避和稳定 `Message-ID` 工具
- [x] 5.5 扩展 `NotificationWorker` 处理日报 pending/retrying 批次，并在启动时恢复中断的 sending 状态
- [x] 5.6 保持日报与即时增量推送的独立幂等域和历史，确保任一投递失败不影响采集任务或另一通知类型
- [x] 5.7 扩展统一投递状态查询，使日报运行、分片、明细、失败摘要和重试结果可在推送管理与任务中心查看

## 6. FastAPI、权限与审计

- [x] 6.1 增加日报规则 CRUD、选项、窗口预览、补跑、运行列表、运行详情和失败重试 API
- [x] 6.2 为 API 增加字段级错误响应、分页、稳定排序和北京时间展示，同时保留 UTC 机器可读值
- [x] 6.3 强制 `notifications` 页面读权限和管理员写权限，禁止普通用户读取完整收件人、预览敏感范围、补跑或重试
- [x] 6.4 为规则变更、启停、删除、补跑和重试写入不含凭证与完整收件人列表的审计日志
- [x] 6.5 在系统配置与任务中心 API 中展示脱敏日报默认值、下一运行、窗口、状态、计数和安全错误摘要

## 7. Vue 推送管理页面

- [x] 7.1 在“推送管理”增加“每日时间线邮件”区域及规则列表，完整处理加载、空、无权限和错误状态
- [x] 7.2 实现规则表单中的人物多选、事件类型、收件人、启停和空日报策略，并阻止提交空人物范围
- [x] 7.3 使用时间控件实现规则级发送时间配置，默认 `08:30`，提交规范化 `HH:mm` 并显示下一次北京时间运行
- [x] 7.4 实现“昨天自然日”与“最近 N 小时”配置、有效范围提示及下一/指定日期窗口预览
- [x] 7.5 实现日报运行与投递历史、事件明细、空/错过/失败状态、管理员补跑和终止失败重试交互
- [x] 7.6 完成桌面与移动端布局、可访问名称、键盘焦点、确认提示和敏感收件人脱敏显示

## 8. 后端与前端自动化测试

- [x] 8.1 增加配置默认值、环境覆盖、`HH:mm`/时区/最近小时校验及下一计划时刻单元测试
- [x] 8.2 增加北京时间昨天自然日和最近 N 小时半开窗口、UTC 转换、边界不重复及发送时间修改测试
- [x] 8.3 增加 SQLite 全新初始化、现有库升级、重复迁移和全部唯一约束测试
- [x] 8.4 增加人物/类型/状态筛选、未知时间回退、稳定排序、来源内容、分片和空日报测试
- [x] 8.5 增加正常调度、重复轮询、重启恢复、跨多日停机、指定日期补跑和规则修改竞态测试
- [x] 8.6 使用 mock SMTP 覆盖纯文本/HTML、成功、临时失败、最大重试、worker 恢复和稳定 `Message-ID`，且不连接真实邮件服务
- [x] 8.7 增加 FastAPI 规则 CRUD、预览、补跑、运行/投递查询、admin/user/无页面权限及审计测试
- [x] 8.8 增加 Vue 人物与时间配置、窗口模式、预览、运行状态、补跑/重试、权限和移动端布局测试
- [x] 8.9 更新真实服务冒烟脚本，验证默认无规则不发送、规则页面加载、窗口预览和 mock 投递流程

## 9. 文档、运维与 Jenkins

- [x] 9.1 更新 `README.md` 的系统介绍、页面、配置、部署、运维、访问、日报语义、时间窗口和故障排查章节
- [x] 9.2 更新软件需求规格说明书，补充人物可配置、发送时间可配置且默认 `08:30`、周期可配置且默认昨天自然日、排序与验收标准
- [x] 9.3 更新系统设计说明书，补充日报表关系、日历调度、窗口计算、Outbox、Worker、API、权限和故障边界
- [x] 9.4 复核 `src/JenkinsConfig/Jenkinsfile`，确保执行新增后端、前端、生产构建、冒烟和 OpenSpec 校验且不访问真实 SMTP
- [x] 9.5 验证 Linux/Windows 的 start、status、stop 脚本能启动、显示和停止日报调度器与通知 Worker
- [x] 9.6 更新部署、备份恢复和回滚说明，明确新增 SQLite 表、至少一次 SMTP 语义、长期停机恢复与手工补跑方式

## 10. 完整验证与交付

- [x] 10.1 运行完整后端 pytest、前端测试、前端生产构建、冒烟测试和 OpenSpec 严格校验并修复全部失败
- [ ] 10.2 在安全测试邮箱验证人物范围、可配置发送时间、默认昨日窗口、稳定排序、空日报和失败恢复
- [x] 10.3 复核即时增量推送、登录权限、系统配置、任务中心和时间线功能无回归
- [ ] 10.4 提交并推送通过质量门禁的实现到 GitHub
- [ ] 10.5 手工触发 Jenkins 构建，确认部署与就绪检查成功
- [ ] 10.6 提供构建后的服务地址和验证步骤，请用户确认日报人物、发送时间、窗口、内容顺序与投递结果
