## 1. 配置与依赖

- [x] 1.1 在后端默认配置和 `src/config/app.json` 中增加 `notifications.email` 全部字段、禁用默认值及字段校验
- [x] 1.2 扩展环境变量加载与有效配置来源跟踪，支持文件/环境配置被数据库页面非空字段逐项覆盖
- [x] 1.3 增加 `cryptography` 锁定依赖，实现外部主密钥校验、Fernet 加解密、页面密码保持/清除语义和统一脱敏
- [x] 1.4 实现 SMTP 地址、收发件人、传输安全、超时、邮件分片与重试参数的规范化和字段级错误

## 2. SQLite 数据迁移

- [x] 2.1 增加向后兼容 schema 迁移，创建 `notification_settings`、`notification_rules` 和 `notification_rule_tasks`
- [x] 2.2 创建 `task_run_events` 并添加任务运行、事件和时间索引
- [x] 2.3 创建可分片的 `email_delivery_batches`、`email_delivery_items`、状态索引及任务运行/事件/收件人幂等唯一约束
- [x] 2.4 验证从现有数据库升级、全新数据库初始化和重复执行迁移均不修改历史业务数据

## 3. 后端增量选择与 Outbox

- [x] 3.1 调整事件分析返回值，在新事件插入成功时返回 ID，同时保持现有事件计数和去重行为
- [x] 3.2 在采集任务路径中写入 `task_run_events`，确保旧事件追加证据、人工录入和维护重分析不被标记为本次新事件
- [x] 3.3 实现规则查询与任务/事件类型匹配，在任务提交后按有效收件人、稳定事件顺序和每封上限创建分片 outbox
- [x] 3.4 对重叠规则和重复 enqueue 使用数据库唯一约束取并集，并在任务日志记录候选、排队、跳过和安全错误计数
- [x] 3.5 隔离 outbox 创建异常，确保邮件排队失败不回滚事件或改写采集任务成功状态

## 4. 邮件生成、发送与重试 Worker

- [x] 4.1 新增邮件模块，生成稳定 `Message-ID`、主题、UTF-8 纯文本/HTML 正文和确定性事件顺序
- [x] 4.2 在邮件中渲染人物、事件类型、标题、摘要、北京时间、地点、状态、具体来源及可选配置化详情链接
- [x] 4.3 实现 SMTP、STARTTLS 和 SMTPS 发送，凭证只在发送时解析且异常经过分类、清洗和截断
- [x] 4.4 实现 `NotificationWorker` 的批次领取、事务外发送、成功/跳过状态写回、指数退避、最大尝试和终止清理
- [x] 4.5 将 worker 接入 FastAPI lifespan，使采集调度关闭时仍可恢复 pending/retrying 批次并安全停止
- [x] 4.6 实现发送前事件可见性检查，跳过已删除/驳回项目并在无可投递项目时标记整个批次 `skipped`

## 5. FastAPI、权限与审计

- [x] 5.1 增加 `notifications` 页面权限键，保持管理员全权限并为普通用户提供显式授权
- [x] 5.2 实现邮件有效配置读取、管理员更新/清除页面覆盖和测试发送 API，返回脱敏值、健康状态和逐字段来源
- [x] 5.3 实现推送选项及规则列表、创建、更新、启停和删除 API，并校验任务与事件类型非空选择
- [x] 5.4 实现投递批次分页列表、详情和管理员手工重试 API，保留幂等明细
- [x] 5.5 对配置/规则变更、测试发送和手工重试写入无敏感信息的审计日志，并对所有写接口强制管理员权限
- [x] 5.6 扩展任务中心 API 上下文，使任务运行可查看邮件排队数和最近推送失败摘要

## 6. Vue 推送管理页面

- [x] 6.1 增加“推送管理”导航、路由状态、页面权限隐藏和无权限处理
- [x] 6.2 实现脱敏的生效配置与字段来源展示，以及仅管理员可用的邮件配置、密码保持/清除表单
- [x] 6.3 实现采集任务多选、事件类型多选、规则新增/编辑/启停/删除和完整加载/空/错误状态
- [x] 6.4 实现测试邮件操作及明确的成功、缺失配置、认证失败和网络失败反馈
- [x] 6.5 实现投递历史分页、状态/任务筛选、北京时间、错误摘要、批次事件详情和管理员失败重试
- [x] 6.6 在任务中心显示排队/失败信息并链接到对应推送投递记录，完善桌面和移动端样式

## 7. 自动化测试与自测

- [x] 7.1 增加配置合并、字段来源、清除回退、校验、脱敏、加密存储和缺失主密钥单元测试
- [x] 7.2 增加迁移、task-run 新事件关联、旧事件去重排除、规则筛选、重叠规则和分片幂等测试
- [x] 7.3 使用本地 mock SMTP 覆盖纯文本/HTML 内容、STARTTLS/SMTPS、成功、临时失败、最大重试、重启恢复和稳定 `Message-ID`
- [x] 7.4 增加 FastAPI 配置、规则、测试发送、投递读取/重试、admin/user/无页面权限及审计集成测试
- [x] 7.5 增加 Vue 页面配置、规则操作、投递状态、失败反馈、权限导航和北京时间展示测试
- [x] 7.6 更新真实服务冒烟脚本，验证登录、页面加载、禁用邮件默认行为及 mock 邮件测试流程且不连接真实邮箱
- [x] 7.7 运行完整后端 pytest、前端测试、前端生产构建和 OpenSpec 校验并修复全部失败

## 8. 文档、运维与 Jenkins

- [x] 8.1 更新 `软件需求规格说明书.md`，加入邮件配置优先级、任务/事件类型规则、增量与幂等、权限、安全、重试和验收标准
- [x] 8.2 更新 `系统设计说明书.md`，加入配置合并、加密凭证、SQLite 表关系、outbox、worker、API 和故障边界
- [x] 8.3 更新 `README.md` 的能力、页面、配置、环境变量、部署、运维、访问和故障排查章节
- [x] 8.4 复核 `src/JenkinsConfig/Jenkinsfile`，确保安装锁定依赖、运行新增后端/前端/冒烟测试且不访问真实 SMTP
- [x] 8.5 验证 Linux/Windows 的 start、status、stop 脚本可启动和停止通知 worker，且日志/PID/配置保留策略不变
- [x] 8.6 更新备份恢复与密钥轮换说明，明确 SQLite 仅含密文、主密钥需独立保管及 SMTP 至少一次投递边界

## 9. 交付验证

- [x] 9.1 在安全测试邮箱上完成页面配置优先于 `app.json`、测试发送和所选任务/事件类型的端到端增量推送验证
- [x] 9.2 验证任务重跑、应用重启、重叠规则和 SMTP 故障恢复不重复创建投递项且不影响采集数据
- [ ] 9.3 提交并推送通过全部质量门禁的变更到 GitHub
- [ ] 9.4 手工触发 Jenkins 构建并确认部署、就绪检查和推送 worker 健康
- [ ] 9.5 提供构建后的服务地址与验证步骤，请用户确认邮件配置、筛选范围、内容和增量语义符合预期
